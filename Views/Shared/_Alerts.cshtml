@if (TempData["SuccessMessage"] != null)
{
    <div class="fixed top-4 right-4 z-50 max-w-md w-full animate-slide-in" id="successAlert">
        <div class="flex items-center p-4 text-sm text-green-800 bg-green-100 rounded-lg shadow-lg border border-green-200"
             role="alert">
            <svg class="w-5 h-5 mr-3 text-green-500 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span class="font-medium">@TempData["SuccessMessage"]</span>
            <button type="button"
                    class="ml-auto -mx-1.5 -my-1.5 text-green-500 bg-green-100 rounded-lg focus:ring-2 focus:ring-green-400 p-1.5 hover:bg-green-200 inline-flex items-center justify-center h-8 w-8 shrink-0"
                    onclick="this.closest('div[role=alert]').remove()">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
        </div>
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="fixed top-4 right-4 z-50 max-w-md w-full animate-slide-in" id="errorAlert">
        <div class="flex items-center p-4 text-sm text-red-800 bg-red-100 rounded-lg shadow-lg border border-red-200"
             role="alert">
            <svg class="w-5 h-5 mr-3 text-red-500 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span class="font-medium">@TempData["ErrorMessage"]</span>
            <button type="button"
                    class="ml-auto -mx-1.5 -my-1.5 text-red-500 bg-red-100 rounded-lg focus:ring-2 focus:ring-red-400 p-1.5 hover:bg-red-200 inline-flex items-center justify-center h-8 w-8 shrink-0"
                    onclick="this.closest('div[role=alert]').remove()">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
        </div>
    </div>
}

@if (ViewBag.SuccessMessage != null)
{
    <div class="fixed top-4 right-4 z-50 max-w-md w-full animate-slide-in" id="viewBagSuccessAlert">
        <div class="flex items-center p-4 text-sm text-green-800 bg-green-100 rounded-lg shadow-lg border border-green-200"
             role="alert">
            <svg class="w-5 h-5 mr-3 text-green-500 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span class="font-medium">@ViewBag.SuccessMessage</span>
            <button type="button"
                    class="ml-auto -mx-1.5 -my-1.5 text-green-500 bg-green-100 rounded-lg focus:ring-2 focus:ring-green-400 p-1.5 hover:bg-green-200 inline-flex items-center justify-center h-8 w-8 shrink-0"
                    onclick="this.closest('div[role=alert]').remove()">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
        </div>
    </div>
}

@if (ViewBag.ErrorMessage != null)
{
    <div class="fixed top-4 right-4 z-50 max-w-md w-full animate-slide-in" id="viewBagErrorAlert">
        <div class="flex items-center p-4 text-sm text-red-800 bg-red-100 rounded-lg shadow-lg border border-red-200"
             role="alert">
            <svg class="w-5 h-5 mr-3 text-red-500 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span class="font-medium">@ViewBag.ErrorMessage</span>
            <button type="button"
                    class="ml-auto -mx-1.5 -my-1.5 text-red-500 bg-red-100 rounded-lg focus:ring-2 focus:ring-red-400 p-1.5 hover:bg-red-200 inline-flex items-center justify-center h-8 w-8 shrink-0"
                    onclick="this.closest('div[role=alert]').remove()">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
        </div>
    </div>
}

<style>
    @@keyframes slideIn {
        from {
            transform: translateX(100%);
            opacity: 0;
        }

        to {
            transform: translateX(0);
            opacity: 1;
        }
    }

    @@keyframes fadeOut {
        from {
            opacity: 1;
            transform: translateX(0);
        }

        to {
            opacity: 0;
            transform: translateX(100%);
        }
    }

    .animate-slide-in {
        animation: slideIn 0.3s ease-out;
    }

    .animate-fade-out {
        animation: fadeOut 0.3s ease-in forwards;
    }
</style>

<script>
    // Auto-close alerts after 2 seconds
    document.addEventListener('DOMContentLoaded', function () {
        // Select all alert divs
        const alerts = document.querySelectorAll('[id$="Alert"]');

        alerts.forEach(function (alert) {
            if (alert) {
                setTimeout(function () {
                    // Add fade out animation
                    alert.classList.add('animate-fade-out');
                    // Remove after animation completes
                    setTimeout(function () {
                        if (alert.parentNode) {
                            alert.remove();
                        }
                    }, 300);
                }, 2000); // 2 seconds
            }
        });
    });
</script>